# Workstation Usage

Use `work-status` for local posture, `work-context` for ownership boundaries, `work-docs` for reading order, `work-verify` for local verification, and `work-release-check` for honest release scaffolding. Treat scaffold commands as partial by design.
