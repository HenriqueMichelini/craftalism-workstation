# Implement

Implement the smallest correct change that respects ownership boundaries. Do not place shared contract definitions into consumer repos. Update repo-local docs only when they reflect behavior this repo actually owns or consumes.
