# Commit Standard

Craftalism commits must match `<type>(<scope>): <summary>`. Use the repo-mapped canonical scope and only the allowed types: `feat`, `fix`, `refactor`, `perf`, `docs`, `test`, `ci`, `chore`, `security`.
